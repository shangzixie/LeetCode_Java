```
keys* #all key. it is useless in work. because keys size is huge.

dbsize #the number of key

exists key #exmination key

del key # delete key

get key #return value

expire key seconds # set valid time of key

ttl key # get the expire time of key

persist key #permanent key

type key # the type of key
```

